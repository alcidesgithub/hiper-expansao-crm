# Database Configuration
# Para desenvolvimento local, use localhost. Para Docker Compose / Coolify, use o nome do service.
POSTGRES_USER="postgres"
POSTGRES_PASSWORD="postgres"
POSTGRES_DB="hiperfarma_crm"
DATABASE_URL="postgresql://postgres:postgres@localhost:5432/hiperfarma_crm?schema=public"

# Cache (Redis) — usado para rate limiting; sem esta variavel, rate limit opera sem Redis
# Para Docker Compose / Coolify, use "redis://redis:6379"
REDIS_URL="redis://localhost:6379"

# Authentication (NextAuth)
# Generate a secret with `openssl rand -base64 32`
AUTH_SECRET="change_me_to_a_secure_random_string"
NEXTAUTH_SECRET="change_me_to_a_secure_random_string"
NEXTAUTH_URL="http://localhost:3000"
AUTH_TRUST_HOST="true"

# App URL (public-facing)
NEXT_PUBLIC_APP_URL="http://localhost:3000"

# Email (Resend)
RESEND_API_KEY="re_your_api_key"
RESEND_FROM_EMAIL="noreply@expansao.hiperfarma.com.br"

# Microsoft Teams Integration (opcional — usa SDK Microsoft Graph)
# Nomes alternativos aceitos: MICROSOFT_TENANT_ID, MICROSOFT_CLIENT_ID, MICROSOFT_CLIENT_SECRET
MS_TEAMS_CLIENT_ID=""
MS_TEAMS_CLIENT_SECRET=""
MS_TEAMS_TENANT_ID=""
MS_TEAMS_GRAPH_SCOPE="https://graph.microsoft.com/.default"
MS_TEAMS_WEBHOOK_URL="http://localhost:3000/api/integrations/teams/webhook"
MS_TEAMS_WEBHOOK_CLIENT_STATE="change_me_to_another_secure_random_string_at_least_32_chars"
TEAMS_SYNC_CRON_TOKEN="change_me_to_a_secure_random_string"

# Health endpoint (optional, recommended in production to read detailed status)
HEALTHCHECK_TOKEN="change_me_to_a_secure_random_string"

# Seed (required in production deploys)
SEED_DEFAULT_PASSWORD="change_me_please_123"

# Storage
UPLOAD_DIR="./uploads"

# Environment
NODE_ENV="development"
